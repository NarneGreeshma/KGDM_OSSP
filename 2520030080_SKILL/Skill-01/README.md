# OSSP Skill-01

## Title
Linux Environment Setup, Build System, Process Management and System Call Analysis

## Objective

To install and configure a Linux development environment, set up GCC and Git, understand shell architecture, create an initial Makefile, and analyze processes using fork(), exec(), parent-child relationships, process tree inspection, and system call tracing.

---

## 1. Linux Environment Setup

Ubuntu Linux was configured using WSL.

The following command was used to verify the Linux environment:

```bash
uname -a
